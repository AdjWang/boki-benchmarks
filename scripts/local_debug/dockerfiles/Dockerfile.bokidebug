# FROM ubuntu:focal as builder
# ENV DEBIAN_FRONTEND=noninteractive
# ENV TZ=America/Chicago
# RUN apt update && \
#     apt upgrade -y && \
#     apt install -y g++ clang make cmake pkg-config autoconf automake libtool curl unzip

# WORKDIR /boki
# COPY . .
# RUN CC=clang CXX=clang++ ./build_deps.sh
# RUN CXX=clang++ make -j $(nproc)

FROM ubuntu:focal as release

# install curl for opentelemetry which requires libcurl.so
# RUN apt update && \
#     apt install -y curl

COPY ./worker /src/boki/worker
COPY ./slib /src/boki/slib

# COPY ./bin/debug/libprofiler.so /boki/libprofiler.so
COPY ./lib/shared_index/bin/debug/libindex.so /boki/libindex.so
COPY ./bin/debug/gateway    /boki/gateway
COPY ./bin/debug/engine     /boki/engine
COPY ./bin/debug/launcher   /boki/launcher
COPY ./bin/debug/sequencer  /boki/sequencer
COPY ./bin/debug/storage    /boki/storage
COPY ./bin/debug/controller /boki/controller

WORKDIR /boki
